 #include "main.h"
/** - int factorial(int n) - prints the factorial of n
 * - @n - some random number
 * - return always 0
 */
int factorial(int n)
{
	if(n < 0)
		return(-1);
	else
		printf("n!:%d",factorial(n(n-1)));
	return(0);
}
